local ComponentType = {
    AND = "AND",
    OR = "OR",
    NOT = "NOT",
    XOR = "XOR",
    NAND = "NAND",
    NOR = "NOR",
    XNOR = "XNOR",
    REGISTER = "REGISTER",
    INPUT = "INPUT",
    OUTPUT = "OUTPUT",
    DELAY = "DELAY",
}

return ComponentType